{
  "name": "garden-manager",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=backend & npm run dev --workspace=frontend",
    "build": "npm run db:generate && npm run build --workspace=shared && npm run build --workspace=backend && npm run build --workspace=frontend",
    "test": "npm run test --workspace=backend && npm run test --workspace=frontend",
    "lint": "npm run lint --workspace=backend && npm run lint --workspace=frontend",
    "typecheck": "npm run db:generate && npm run build --workspace=shared && npm run typecheck --workspace=backend && npm run typecheck --workspace=frontend",
    "check:whitespace": "node scripts/check-whitespace.mjs",
    "check:migrations": "node scripts/check-released-migrations.mjs",
    "check:secrets": "node scripts/scan-committed-secrets.mjs",
    "check:repository": "npm run check:whitespace && npm run check:migrations && npm run check:secrets",
    "check:quality": "npm run check:repository && npm run typecheck && npm test && npm run lint && npm run build",
    "db:generate": "npm run db:generate --workspace=backend",
    "db:migrate": "npm run db:migrate --workspace=backend",
    "db:seed": "npm run db:seed --workspace=backend"
  },
  "engines": {
    "node": ">=20"
  }
}
